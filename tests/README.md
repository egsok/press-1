# Offline test suites

Seven suites cover the protocol logic without a live Claude Code / Codex session. Sandboxes live in `%TEMP%\press-1-tests\` — the repo tree is never written to.

| Suite | What it covers | Run |
|-------|----------------|-----|
| `hook.test.js` (68) | Claude hook: host fingerprint classification, decision channel for editor-terminal/panel/WT/conhost (allow / always-echo / deny), `editor_exe` capture, 60-min window + early-exit, picker liveness beacon, readable tool summaries (ExitPlanMode → label, or plan text with `#`/`##` headings stripped — not JSON), **`tool_input_full` (newline-preserving, uncapped-at-200) for the popup expand affordance**, teardown interplay | `node tests/hook.test.js` |
| `codex-hook.test.js` (86) | Codex hook: `agent:"codex"` pending, Codex `classifyHost` (panel/WT/vsterm/conhost, no `CLAUDE_CODE_ENTRYPOINT`), 3-button options, allow/deny, **always → `prefix_rule` appended to default.rules** (tokenize + backslash escape + exact-line dedup; empty command → no rule), no `updatedPermissions` echo, **proxy-channel mute** (`PRESS1_PROXY` in env → silent exit 0, no pending/sound; empty string = not muted), **two-phase hybrid** (15 s window → atomic rewrite to `native_control`, decision-vs-rewrite deadline race, per-host mode matrix, conhost decision-only, window clamps), **opt-in native-control panel mode** (`native_control:true`, no `decision_file`, immediate pass-through), **same-session cleanup** (a new prompt deletes the session's stale pendings), **`tool_input_full` for expand**, early-exit, fail-safe exit-0 | `node tests/codex-hook.test.js` |
| `ahk-harness.ahk` (198) | pending parsing (anchor guards), row build + FIFO, decision_file + `editor_exe` wiring, `wait_until` cutoff, liveness gates (dead window, dead hook PID, dead picker beacon), editor-terminal/panel/standalone rows, digit→word mapping; **Codex hybrid phase-2 rows** (native_control survives a dead hook_pid and flows through the standalone push, `NativeRouteForHost` routing, `IsCodexActionTitle`/`PickCodexActionHwnd`/`ResolveCodexStandaloneTarget` WT guards, panel uniqueness guard, TTL deletion, `AllDismissed` subset suppression, `ResolveCodexPanelFallback` count-of-one panel fallback); **stack order keyed to the JSON `timestamp`** (survives the phase 1→2 rewrite); **`agent` parse + `P1_HostBadge(agent,host)` codex·* variants (Claude badges unchanged) + `IsPanelHost`**; **`P1_StackYTargets` clean fixed-gap stack math (no overlap at differing heights), `P1_HeaderBottom` N-line header height, `P1_CmdTruncated` expand predicate, expand-state map**; **experimental Codex proxy channel** (`ReadAllProxyPending` adapter — derived `project_name`, agent/channel/host, 3-label vs 2-label options, dropped when cwd missing, torn-JSON skip, non-`*.pending.json` ignored; `WriteProxyDecision` accept/cancel + byte-exact amendment echo; `DispatchByKind` `channel:"proxy"` routing; reconcile-on-delete; `codex·proxy` badge) | `& "C:\Program Files\AutoHotkey\v2\AutoHotkey64.exe" tests\ahk-harness.ahk` → results in `%TEMP%\press-1-tests\ahk-out.txt` |
| `merge-hooks.test.js` (27) | settings.json merge: clean machine, foreign hooks preserved, idempotency, command/timeout upgrade (PermissionRequest ≥ 3660), fail-loud on invalid JSON | `node tests/merge-hooks.test.js` |
| `codex-merge.test.js` (59) | `~/.codex/hooks.json` wrapper-schema merge (foreign top-level keys removed write-once to sidecar; **timeout set-exact 70 — overwrites an already-installed 30**), idempotency, fail-loud; pure `upsertTrust` (config.toml EOF-append, in-place hash replace, single-quote-key guard, later `[windows]`/`[projects]` preserved) + `isOurHook` matching. App-server spawn skipped via `PRESS1_CODEX_SKIP_TRUST=1` | `node tests/codex-merge.test.js` |
| `codex-mitm.test.js` (25) | Experimental proxy wrapper (`codex-mitm.js`) against a scripted fake app-server: pending publish (atomic, `<pid>-<reqId>` naming, schema `press1.codex.proxy/1`), decision inject (accept / cancel / amendment object verbatim), cleanup on `serverRequest/resolved` and on exit, passthrough (native answer, zero injects), byte-exact relay incl. non-JSON lines, ctrl-dir degradation (pass-through + **no `PRESS1_PROXY`**), `PRESS1_PROXY=1` in child env when ctrl is up, fail-loud exit 127 without a bundle, exit-code propagation | `node tests/codex-mitm.test.js` |
| `codex-proxy-settings.test.js` (52) | JSONC-safe `chatgpt.cliExecutable` editor: splice enable/disable preserve comments/formatting byte-exact outside the splice, idempotency both ways, enable↔disable round-trip, write-once backup, WSL-mode refusal (exit 2), invalid-JSONC refusal (original untouched), missing settings.json fail-loud | `node tests/codex-proxy-settings.test.js` |

Notes:

- The AHK harness `#Include`s the real `press-1.ahk` and then redirects its directories to the sandbox — run it from the repo so the relative include resolves. It exercises parsing/lifecycle functions only; no popups are shown.
- The hook suites spawn the real `permission-request.js` / `codex-permission-request.js` as a child with an isolated `TEMP` (stdin payload → stdout decision). Permission hooks block on the decision channel (Codex always; the suite writes the decision file then reaps), so they run a few seconds.
- `codex-merge.test.js` skips the live `codex app-server` spawn (`PRESS1_CODEX_SKIP_TRUST=1`) and unit-tests the pure `upsertTrust`/`isOurHook` exports directly — no Codex CLI needed.
- `codex-native-control-smoke.ps1` is a **manual live smoke helper** for the opt-in Codex panel native-control path. Run it, trigger a real approval in the Codex VS Code panel, press `Ctrl+Win+1/2/3`, then press Enter in the helper console; it logs whether a `native_control:true` pending appeared, whether it had no `decision_file`, whether the hook released, and whether the pending disappeared after the confirmed hotkey step. Use `-NoPrompt` only for syntax/fail-path checks.
- `gdip-smoke.ahk` is a separate **runtime** renderer smoke (not in the table above): it drives the real GDI+ stack — builds a 3-card stack, moves the active ring, toggles a card's expand (rebuild at a taller height), dismisses, and re-shows — catching runtime errors the syntax-only harness can't. PASS/FAIL is written to `%TEMP%\press-1-tests\gdip-smoke.txt`; it briefly shows real cards bottom-right (~2 s). Run: `& "C:\Program Files\AutoHotkey\v2\AutoHotkey64.exe" tests\gdip-smoke.ahk`.
- Run all seven (plus the gdip smoke) after any change to `permission-request.js`, `codex-permission-request.js`, `session-teardown.js`, `press-1.ahk`, `merge-hooks.js`, `merge-codex-hooks.js`, `codex-mitm.js`, or `codex-proxy-settings.js`.
