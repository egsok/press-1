<div align="center">

# ⌨️ press-1

**Отвечайте на запросы разрешений AI-агента одним нажатием — из любого окна, на любом мониторе, не переключая фокус.**

**Русский** · [English](README.md)

[![Latest Release](https://img.shields.io/github/v/release/egsok/press-1?style=for-the-badge)](../../releases/latest)
![Platform](https://img.shields.io/badge/Platform-Windows-blue?style=for-the-badge)
![Agents](https://img.shields.io/badge/Agents-Claude%20Code%20%C2%B7%20Codex-452BA6?style=for-the-badge)
![Editors](https://img.shields.io/badge/Editors-VS%20Code%20%C2%B7%20Cursor%20%C2%B7%20Windsurf-2496ED?style=for-the-badge)
[![License: MIT](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)](LICENSE)

![press-1](assets/press-1.png)

</div>

---

AI-агенты вроде Claude Code и Codex останавливаются и спрашивают разрешение перед чем-то важным — *выполнить команду? изменить файл? удалить?* Загвоздка в том, что этот запрос появляется там, где работает агент, — а это редко то окно, на которое вы сейчас смотрите. В итоге агент стоит и ждёт, вы теряете время и фокус, разыскивая запрос, — а то и вовсе его не замечаете, и агент просто простаивает.

**press-1 показывает все ждущие запросы в одном маленьком окне поверх остальных, проигрывает мягкий звук и даёт ответить одним глобальным хоткеем — не отрываясь от того, чем заняты, — или мышкой прямо в окне. В любом случае фокус остаётся там, где был.** Решение всегда за вами; press-1 лишь убирает беготню по окнам между вами и запросом.

![press-1 на трёх мониторах](assets/press-1-3-monitors.png)

## Зачем это нужно

- **Несколько мониторов** — Запрос всплыл на экране, на который вы не смотрите. Обычно это: тянуться к мыши → кликнуть в окно → ответить — несколько действий через весь стол. press-1 сворачивает их в один хоткей — без мыши, без смены фокуса.
- **Один экран, много окон** — Редактор или терминал скрыт под другими окнами, и вы не видите запрос. Агент ждёт, пока вы думаете, что он работает, — чистая потеря времени. press-1 гарантирует, что вы всегда его увидите *и* услышите.
- **Даже на ноутбуке** — Запустили задачу и открыли YouTube поверх терминала. Звук и попап подскажут ровно в тот момент, когда вы нужны, и вы согласуете, не закрывая видео.
- **Контекст прямо в попапе** — Он показывает, что именно спрашивают (команду, файл, вопрос), так что в большинстве случаев можно решить, вообще не заглядывая в терминал. Нужны детали? Одна кнопка перенесёт вас в нужное окно.
- **Мягкий, ненавязчивый звук** даёт понять, что нужно ваше внимание — и отключается в один клик, если хотите тишины.
- **Без клавиатуры тоже** — С запросами можно работать целиком мышью, кликая прямо в попапе.

## Попап

![Попап press-1](assets/press-1-popup.png)

- Стопка карточек в две краски на чернильно-фиолетовой стене, по одной на каждый ждущий запрос — старейшая сверху. Хоткеи отвечают **активной** карточке: её заголовок напечатан чётко, а в углу горит маджентовая метка приводки; карточки в очереди слегка «плывут» несведением, как оттиски в ожидании пресса. `Ctrl+Win+↑/↓` двигает выбор.
- Каждая карточка показывает, какой агент спрашивает (плашка `CLAUDE` / `CODEX`), и одно ждущее действие — `Bash(npm test)`, `Write(src/config.ts)` и так далее. У длинных команд есть **Expand ▾** (`Ctrl+Win+E`) — разворачивает полную команду прямо на карточке.
- Кнопки повторяют реальную раскладку на 2 или 3 опции и кликабельны. `🔍` фокусирует окно запроса, **не** отвечая.
- Карточка исчезает в тот момент, когда запрос решён где угодно — отвечен *или* отменён руками в терминале.

## Хоткеи

Из коробки: **Ctrl+Win+1 / 2 / 3** отвечают на верхний запрос (опция 1 / 2 / 3), а **Ctrl+Win+Esc** прячет попап. Кнопки всегда повторяют реальные опции запроса, так что цифра никогда не значит не то, что вы видите.

Хотите выделенные клавиши? Назначьте их на **любое железо** — macro pad, мини-клавиатуру, консоль в духе stream-deck, кейпад с экранчиками. press-1 ещё слушает `F13`–`F24`, так что подойдёт всё, что умеет их слать. Как вам удобно.

<table align="center">
  <tr>
    <td align="center">
      <img src="assets/logitech-mx-console.jpg" alt="Logitech MX Creative Console с press-1" height="450"><br>
      <sub><b>Моя живая раскладка</b> — Logitech MX Creative Console; верхний ряд (CC&nbsp;1/2/3) — это press-1.</sub>
    </td>
    <td align="center">
      <img src="assets/macro-pad.png" alt="Простой 3-кнопочный macro pad" height="450"><br>
      <sub><b>Или по-минимуму</b> — 3-кнопочный macro pad: дёшево, компактно и полностью закрывает задачу.</sub>
    </td>
  </tr>
</table>

## Это не auto-approver

Важный момент: **press-1 никогда не отвечает за вас.** Он ничего не одобряет автоматически, не обходит запросы, не «доверяет» командам. Каждый запрос остаётся осознанным решением — одно нажатие (или клик) на каждый. press-1 лишь делает это решение мгновенно доступным. Все три опции — ровно как их предлагает сам запрос: **Allow**; **Always allow**, который сохраняет правило точно так же, как родная кнопка; и **Deny**, который отправляет агенту корректное сообщение «отклонено пользователем».

## Где работает

Claude Code запускается на Windows в нескольких местах, и press-1 покрывает каждое:

- **Ваш редактор — VS Code, [Cursor](https://cursor.com/) или [Windsurf](https://windsurf.com/)** — и встроенное окно разрешения, и интегрированный терминал. Cursor и Windsurf — форки VS Code; press-1 работает со всеми тремя одинаково.
- **Windows Terminal** — и отдельные окна `cmd` / PowerShell

Не всякий запрос — простое «да/нет»: согласовать план или выбрать из списка опций. press-1 не угадывает за вас: он выносит такие в попап как строку внимания, а хоткей переносит вас прямо в нужное окно, чтобы вы ответили там.

## Работает и с OpenAI Codex

Если вы пользуетесь ещё и [Codex CLI](https://github.com/openai/codex), press-1 обслуживает его подтверждения команд точно так же: тот же попап, те же хоткеи, плашка `CODEX` на карточке — всегда видно, какой агент спрашивает. Установщик настраивает это сам: Codex-вариант хука копируется в `~\.codex\hooks\` и регистрируется в `~\.codex\hooks.json` тем же безопасным мержем, что и у Claude. Запросы Codex в терминале редактора, Windows Terminal и панели Codex в VS Code работают из коробки (проверено на codex-cli 0.144.x). Одна честная оговорка: **Always allow** для Codex сохраняет правило по префиксу команды — для части сложных команд Codex его игнорирует и в следующий раз просто спросит снова.

Обработку Codex можно в любой момент включить/выключить из меню трея (**Active for → Codex**); у Claude такой же переключатель.

### Панель Codex нативно — экспериментальный прокси-канал (opt-in)

Панель Codex в VS Code обычно едет по тому же hook-каналу. Но для неё есть и более глубокая, нативная интеграция: маленький stdio-прокси (`codex-mitm`) встаёт между расширением и его встроенным `codex app-server`, зеркалит запросы подтверждений в попап и инжектит ваш ответ обратно — карточка в панели гаснет мгновенно, ровно как от клика, включая полноценный серверный **Always allow**.

Включение / выключение из папки репозитория (нужен Node.js; после любого из двух — перезагрузите окна VS Code):

```powershell
.\enable-codex-proxy.ps1     # собирает обёртку-exe и направляет на неё chatgpt.cliExecutable
.\disable-codex-proxy.ps1    # снимает настройку — панель возвращается на встроенный Codex
```

Это **эксперимент, выключен по умолчанию, use at your own risk** — честные оговорки:

- Держится на настройке расширения `chatgpt.cliExecutable`, которую OpenAI помечает **«DEVELOPMENT ONLY»**. Обновление расширения может сломать её в любой момент; обёртка спроектирована падать *громко*, а disable-скрипт откатывает всё (write-once бэкап вашего `settings.json` сохраняется).
- Обёртка стоит на критическом пути панели. Она построена по принципу relay-first: если ломается часть press-1, кадры продолжают проходить и панель работает — теряется только попап-фича. Если панель всё же забарахлила: disable-скрипт + перезагрузка окна.
- **Deny** через прокси обрывает текущий тёрн агента (жёсткий стоп). Родная кнопка Deny в панели отклоняет *мягко* и даёт агенту продолжить — она идёт по каналу, до которого прокси не дотягивается. Хотите, чтобы агент продолжил работу с вашим отказом, — жмите родную кнопку.

Дизайн и сценарии отказа описаны в [docs/DESIGN-CODEX-PROXY.md](docs/DESIGN-CODEX-PROXY.md).

## Установка

**Быстрый путь — одна строка.** Она сама ставит нужное (AutoHotkey v2 и Node.js — через `winget`, только если их нет), а затем и press-1:

```powershell
irm https://raw.githubusercontent.com/egsok/press-1/main/bootstrap.ps1 | iex
```

Готово. (Если обновляетесь с прежней версии press-1, установщик попросит один раз перезагрузить окна редактора.)

**Хотите сперва прочитать код?** Склонируйте репозиторий, посмотрите его и запустите тот же установщик:

```powershell
git clone https://github.com/egsok/press-1
cd press-1
.\install.ps1
```

`install.ps1` проверит **[AutoHotkey v2](https://www.autohotkey.com/)** и **[Node.js](https://nodejs.org/)**, предложит поставить недостающее через `winget`, всё настроит и (пере)запустит AutoHotkey. Готово.

**Требования:** Windows 10/11, Claude Code 2.1.x и редактор — VS Code, Cursor или Windsurf (для сценариев в редакторе). `winget` (встроен в актуальные Windows) нужен для авто-установки зависимостей — если его нет, поставьте две зависимости вручную и перезапустите. Codex CLI — опционален: если он у вас есть, установщик подключит и его.

> **Некогда разбираться?** Отдайте репозиторий своему агенту Claude Code и попросите установить press-1 — он сделает ровно это. Та самая рутина, для которой он и нужен.

<details>
<summary><b>Что на самом деле делает установщик</b></summary>

<br>

- Ставит недостающие зависимости (**AutoHotkey v2**, **Node.js**) через `winget`, спросив, — или громко сообщает, если `winget` недоступен.
- Копирует два хука (`permission-request.js`, `session-teardown.js`) в `~\.claude\hooks\`.
- **Безопасно** мержит свои записи хуков в `~\.claude\settings.json` — трогает только свои записи, остальные ваши хуки и настройки сохраняются, бэкап пишется первым. Невалидный JSON → останавливается громко и ничего не меняет.
- То же для Codex: хуки в `~\.codex\hooks\`, безопасный мерж в `~\.codex\hooks.json` (безвредно, если Codex вы не используете).
- Копирует резидентный скрипт (`press-1.ahk` + иконку трея) и брендовые шрифты попапа (`fonts\`, лицензия OFL — грузятся приватно для процесса, в Windows ничего не устанавливается) в `~\scripts\`, добавляет ярлык в автозагрузку.
- (Пере)запускает AutoHotkey, чтобы новая версия была активна.
- Экспериментальный прокси Codex-панели в установку **не** входит — он включается отдельно через `enable-codex-proxy.ps1` (см. выше).

</details>

## Ограничения (by design)

- **Только Windows**, пока — и Claude Code должен работать на этой же машине. Если он запущен на удалённом сервере (SSH/tmux), press-1 не видит его запросы.
- **60-минутное окно ответа** — после него строка исчезает, и вы отвечаете в терминале/боксе как обычно (запрос всё это время остаётся доступен там, и ответ там всегда срабатывает мгновенно).
- **Auto-режим разрешений:** когда классификатор Claude Code решает сам, запрос не возникает и press-1 молчит — вас ничего не ждало.
- **Выбор из списка, вопросы, подтверждение плана** — здесь press-1 только наводит фокус: цифры не печатаются вслепую в окно, у которого может не быть ожидаемого фокуса.
- **Часть состояний терминала не выносится в попап** — внутренние меню (вроде `/model`) и свободный ввод текста не всплывают; их вы обрабатываете прямо в терминале. press-1 покрывает запросы разрешений плюс подтверждение плана и вопросы с выбором.

## Как это устроено

Хук `PermissionRequest` пишет небольшой pending-файл (с пометкой, где запрос — панель / терминал VS Code / Windows Terminal), проигрывает звук, а резидентный AutoHotkey-скрипт показывает попап. Ответы адресуются **конкретному** запросу — если запрос исчез, ответ отбрасывается, а не печатается заново в невиновное окно.

| Файл репо | Назначение |
|-----------|------------|
| `permission-request.js` | хук: pending-файл, звук, ожидание решения |
| `codex-permission-request.js` | тот же хук в варианте для Codex (`~\.codex\hooks\`) |
| `session-teardown.js` | удаляет запросы, отвеченные в другом месте |
| `press-1.ahk` | резидентный скрипт: хоткеи, попап, роутинг |
| `install.ps1` | установщик (хуки Claude + Codex, шрифты, автозагрузка) |
| `codex-mitm.js` + `enable`/`disable-codex-proxy.ps1` | экспериментальный прокси Codex-панели (opt-in) |

Полный контракт файлового протокола — в [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md). Офлайн-тесты — в [tests/](tests/).

## Решение проблем

| Симптом | Что проверить |
|---------|---------------|
| Попап не появляется | Запущен ли AutoHotkey (иконка в трее)? Есть ли записи хуков в `~\.claude\settings.json` и `node` в `PATH`? |
| Попап есть, но хоткеи не отвечают | `timeout` хука `PermissionRequest` должен быть ≥ 3660 (установщик его выставляет) |
| Запросы из терминала редактора не детектятся | если вы только что обновились — перезагрузите окно редактора один раз (палитра команд `Ctrl+Shift+P` → **Developer: Reload Window**, рус. «Разработчик: Перезагрузить окно»); иначе проверьте, что хук есть в `~\.claude\settings.json` и `node` в `PATH` |
| Запросы Codex не показывают попап | проверьте записи в `~\.codex\hooks.json` и меню трея — **Active for → Codex** должен быть отмечен |
| Панель Codex забарахлила после включения прокси | `.\disable-codex-proxy.ps1`, затем перезагрузите окна VS Code — панель вернётся на встроенный Codex |
| Per-monitor хоткей попадает не в то окно | `Ctrl+Win+D` показывает найденные окна и порядок мониторов |

## Авторы

Сделано ИИ 🤖 · проверено человеком.

Построено на [AutoHotkey v2](https://www.autohotkey.com/) — попап, хоткеи и роутинг целиком на AHK. Попап рисуется vendored-копией GDI+-библиотеки [AHKv2-Gdip](https://github.com/buliasz/AHKv2-Gdip), а хуки работают на [Node.js](https://nodejs.org/). Двухкрасочный вид набран шрифтами [IBM Plex](https://github.com/IBM/plex) и [Unbounded](https://fonts.google.com/specimen/Unbounded) — идут в комплекте под OFL (см. `fonts/OFL-NOTICE.txt`).

## Автор

Сделал [Егор Соколов](https://egorsokolov.ru/) — 10 лет в продукте (Сбер, Рольф, Клаустрофобия). Пишу и экспериментирую с AI-инструментами — в основном Claude Code, Codex и тулинг для разработки.

📣 Мой телеграм про AI без хайпа — нахожу, пробую, ломаю, рассказываю:

[![Telegram](https://img.shields.io/badge/Telegram-%40neiroset__ne__vinovata-26A5E4?style=for-the-badge&logo=telegram&logoColor=white)](https://t.me/+AHKYCN02eONjYTVi)

Другие open-source эксперименты:

- [plan-tango](https://github.com/egsok/plan-tango) — цикл ревью планов Claude ↔ Codex для Claude Code.
- [video-downloader2](https://github.com/egsok/video-downloader2) — простой десктопный загрузчик видео с YouTube, VK и 1000+ сайтов.
- [Handy-nevinovata](https://github.com/egsok/Handy-nevinovata) — персональный форк Handy, офлайн speech-to-text под русский IT.

## Лицензия

MIT — см. [LICENSE](LICENSE). Copyright (c) 2026 Egor Sokolov.
